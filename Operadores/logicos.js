// Operadores lógicos
let a = true;
let b = false;

let c = false;
let d = true;

console.log(a)
console.log(b);
console.log(c)
console.log(d);

// Operador Logico && (and o y)
// Regresa verdadero si ambos operados son verdaderos
console.log(`${a} && ${b} -> ${a && b}`);
console.log(`${c} || ${d} -> ${c || d}`);

// Operador !
// Invierte el valor original (false -> true)
console.log(`${a} -> ${!a}`);